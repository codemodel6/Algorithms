1. [].sort()

배열의 값을 정렬한다. 이때 그냥 sort만 하게 되면 1과 10을 구별하지 못한다. 그렇기 때문에
sort((a,b)=>(a-b)) 를 하게 되면 오름차순 정렬
sort((a,b)=>(b-a)) 를 하게 되면 내림차순 정렬 이다

응용
let arr=[[6, 6], [2, 2], [4, 3]];
arr.sort((a, b)=>(a[0]+a[1])-(b[0]+b[1]));

sort의 시간복잡도는 nlogn 이다 그렇기 때문에 마구 쓰지 말고 투포인터 알고리즘을 쓸 수 있으면 쓰자