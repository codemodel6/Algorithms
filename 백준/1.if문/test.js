let fs = require("fs");
const input = fs.readFileSync("test.txt").toString().trim().split(" ");

let A = Number(parseInt(input[0]));
let B = Number(parseInt(input[1]));
let C = Number(parseInt(input[2]));

